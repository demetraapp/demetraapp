# Platform



## Getting started
After clone do:
git submodule update --init --recursive
and then work inside the submodule you want.
Careful! create the new branch inside the submodule not inside the platform
